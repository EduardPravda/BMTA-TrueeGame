<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient">

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/score_title"
        android:textColor="#FFFFFF"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_marginTop="8dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <Button
        android:id="@+id/btn_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/btn_back"
        android:backgroundTint="@color/btn_back_bg"
        android:textColor="#FFFFFF"
        app:cornerRadius="20dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="16dp"/>

    <ScrollView
        android:id="@+id/scroll_filters_container"
        android:layout_width="180dp"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:layout_marginStart="16dp"
        app:layout_constraintTop_toBottomOf="@+id/tv_title"
        app:layout_constraintBottom_toTopOf="@+id/btn_back"
        app:layout_constraintStart_toStartOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/filter_mode_label"
                android:textColor="#DDDDDD"
                android:textSize="12sp"
                android:layout_marginBottom="4dp"/>

            <Button
                android:id="@+id/filter_all"
                android:layout_width="match_parent"
                android:layout_height="35dp"
                android:text="@string/filter_all"
                android:textSize="11sp"
                android:textColor="@color/btn_primary_text"
                android:backgroundTint="@color/btn_primary_bg"
                app:cornerRadius="18dp"
                android:layout_marginBottom="4dp"/>

            <Button
                android:id="@+id/filter_classic"
                android:layout_width="match_parent"
                android:layout_height="35dp"
                android:text="@string/filter_classic"
                android:textSize="11sp"
                android:textColor="@color/btn_primary_text"
                android:backgroundTint="@color/btn_primary_bg"
                app:cornerRadius="18dp"
                android:layout_marginBottom="4dp"/>

            <Button
                android:id="@+id/filter_time"
                android:layout_width="match_parent"
                android:layout_height="35dp"
                android:text="@string/filter_time"
                android:textSize="11sp"
                android:textColor="@color/btn_primary_text"
                android:backgroundTint="@color/btn_primary_bg"
                app:cornerRadius="18dp"
                android:layout_marginBottom="4dp"/>

            <Button
                android:id="@+id/filter_memory"
                android:layout_width="match_parent"
                android:layout_height="35dp"
                android:text="@string/filter_memory"
                android:textSize="11sp"
                android:textColor="@color/btn_primary_text"
                android:backgroundTint="@color/btn_primary_bg"
                app:cornerRadius="18dp"
                android:layout_marginBottom="16dp"/>

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/filter_diff_label"
                android:textColor="#DDDDDD"
                android:textSize="12sp"
                android:layout_marginBottom="4dp"/>

            <Button
                android:id="@+id/diff_all"
                android:layout_width="match_parent"
                android:layout_height="35dp"
                android:text="@string/diff_all"
                android:textSize="11sp"
                android:textColor="@color/btn_primary_text"
                android:backgroundTint="@color/btn_primary_bg"
                app:cornerRadius="18dp"
                android:layout_marginBottom="4dp"/>

            <Button
                android:id="@+id/diff_easy"
                android:layout_width="match_parent"
                android:layout_height="35dp"
                android:text="@string/diff_easy"
                android:textSize="11sp"
                android:textColor="@color/btn_primary_text"
                android:backgroundTint="@color/btn_primary_bg"
                app:cornerRadius="18dp"
                android:layout_marginBottom="4dp"/>

            <Button
                android:id="@+id/diff_medium"
                android:layout_width="match_parent"
                android:layout_height="35dp"
                android:text="@string/diff_medium"
                android:textSize="11sp"
                android:textColor="@color/btn_primary_text"
                android:backgroundTint="@color/btn_primary_bg"
                app:cornerRadius="18dp"
                android:layout_marginBottom="4dp"/>

            <Button
                android:id="@+id/diff_hard"
                android:layout_width="match_parent"
                android:layout_height="35dp"
                android:text="@string/diff_hard"
                android:textSize="11sp"
                android:textColor="@color/btn_primary_text"
                android:backgroundTint="@color/btn_primary_bg"
                app:cornerRadius="18dp"/>
        </LinearLayout>
    </ScrollView>

    <androidx.cardview.widget.CardView
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp"
        app:cardBackgroundColor="@color/card_background"
        app:cardCornerRadius="24dp"
        app:cardElevation="8dp"
        app:layout_constraintStart_toEndOf="@+id/scroll_filters_container"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tv_title"
        app:layout_constraintBottom_toBottomOf="parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_scores"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="16dp"/>
    </androidx.cardview.widget.CardView>

</androidx.constraintlayout.widget.ConstraintLayout>